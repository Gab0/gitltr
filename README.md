A simple script helper to quickly make commits when many files have been modified, easing the process of making custom messages for each file. This is just to do things faster, in a fun & non professional way.

just run the script under target git repo root.
